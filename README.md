## HOW TO RUN
The code implemented multiagent system must to be launched with the Main class. The different arguments that could be neccesary to change are hardcoded. Therefore, the different strategies, commitment and simulation parameters must be tuned in their inline definition. 

The project must be built and run:

- `java Main`
